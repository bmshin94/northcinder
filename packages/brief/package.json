{
  "name": "@northcinder/brief",
  "version": "0.1.0",
  "description": "NorthCinder buyer's brief: a deterministic decision artifact composed by CODE from ranked results — ≤5 finalists with whyThis derived from ranking reasons, computed tradeoffs, per-cell provenance, rejected appendix, and honest per-store coverage (silent skipping forbidden). Includes the deterministic markdown renderer (universal fallback).",
  "license": "MIT",
  "type": "module",
  "engines": { "node": ">=20" },
  "keywords": ["northcinder", "shopping", "buyers-brief"],
  "publishConfig": { "access": "public" },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "!dist/**/*.map"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@northcinder/protocol": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
