{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "{{publisherNamespace}}/northcinder-mcp-remote",
  "title": "NorthCinder self-hosted read-only MCP bridge",
  "description": "Optional read-only MCP bridge deployed and controlled by its user; the NorthCinder repository owner operates no endpoint.",
  "version": "0.1.0",
  "repository": { "url": "{{repositoryUrl}}", "source": "github" },
  "remotes": [{ "type": "streamable-http", "url": "{{remoteUrl}}", "headers": [{ "name": "Authorization", "description": "Bearer API key generated by the person deploying this bridge.", "isRequired": true, "isSecret": true }] }]
}
