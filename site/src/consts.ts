// Public coordinates are owner-supplied release inputs. An unconfigured local build
// must never manufacture a repository organization or deployed origin.
export { REPOSITORY_URL as REPO_URL } from './public-coordinates';
